import { ProductFormValidator } from './product-form-validator';

describe('ProductFormValidator', () => {
  it('should create an instance', () => {
    expect(new ProductFormValidator()).toBeTruthy();
  });
});
