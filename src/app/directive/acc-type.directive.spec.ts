import { AccTypeDirective } from './acc-type.directive';

describe('AccTypeDirective', () => {
  it('should create an instance', () => {
    const directive = new AccTypeDirective();
    expect(directive).toBeTruthy();
  });
});
